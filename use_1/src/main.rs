use std::fmt::Result;
use std::io::Result as IoResult;
use std::{cmp::Ordering, io};
// use std::io;
// use std::io::Write;
use std::collections::*;
use std::io::{self, Write};

fn main() {
    println!("Hello, world!");
}
